# 환경변수 (절대 커밋 금지 - API 키 포함)
.env

# Node 의존성 (npm install로 재설치 가능)
node_modules/

# 서버 모드 뉴스레터 데이터 (서버마다 별도 관리)
data/newsletters.json

# GitHub Pages 정적 데이터는 커밋 대상 (Actions가 생성)
# public/data/ 는 제외하지 않음

# 로그
*.log
npm-debug.log*

# OS 파일
.DS_Store
Thumbs.db
